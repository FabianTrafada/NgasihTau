# Force LF line endings for shell scripts (critical for Docker)
*.sh text eol=lf

# SQL migrations should also use LF
*.sql text eol=lf

# Keep Windows line endings for Windows-specific files
*.bat text eol=crlf
*.cmd text eol=crlf
